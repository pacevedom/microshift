scenarios:
  # Example: Standard test suite (non-upgrade)
  - name: el96-src@dns
    description: |
      Run DNS test suite against any MicroShift instance.
    suites:
      - suites/standard1/dns.robot
    config:
      network:
        - "default"
      os: "el96"
    destroy_vm: false
